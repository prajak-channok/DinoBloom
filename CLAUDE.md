# DinoBloom — Project Instructions

## Project
- Engine: Godot 4.7 / GDScript
- Game: วางพืชต่อสู้ไดโนเสาร์บน Grid 5 Lanes × 8 คอลัมน์ (Dinosaur เดินขวา→ซ้าย)
- โครงสร้างหลัก: `assets/`, `data/` (`.tres`), `scenes/` (`.tscn`), `scripts/` (`.gd`)

## Source of Truth (ลำดับความเชื่อถือ)
1. Project files จริง (`.gd` / `.tscn`)
2. `.tres` Data Resources ที่ใช้งานจริง
3. เอกสารใน `docs/`
4. ข้อสันนิษฐานของ Agent — ใช้เมื่อไม่มีข้อมูลข้างต้นเท่านั้น

ห้ามสรุปว่าอะไร "เสร็จ" จากเอกสารเพียงอย่างเดียว ต้องตรวจ Project จริงก่อนเสมอ

## หลักการทำงาน (คุมทุก task)
Bias ไปทาง caution มากกว่า speed — งานเล็กจริงๆ ให้ใช้ดุลยพินิจ

**1. คิดก่อนโค้ด**
- บอก assumption ที่ใช้อย่างชัดเจน ถ้าไม่แน่ใจให้ถาม
- ถ้าตีความได้หลายแบบ ให้เสนอทางเลือก ไม่เลือกเองเงียบๆ
- ถ้ามีทางที่ง่ายกว่า พูดออกมา ทักท้วงได้ถ้าเห็นว่าไม่ควรทำแบบที่ขอ
- ถ้าอะไรไม่ชัดเจน หยุด บอกว่าติดตรงไหน แล้วถาม

**2. ความเรียบง่ายมาก่อน**
- เขียนโค้ดขั้นต่ำสุดที่แก้ปัญหาได้ ไม่ใส่ feature เกินคำขอ
- ไม่สร้าง abstraction/config/flexibility ที่ไม่มีใครขอ
- ไม่ทำ error handling สำหรับ scenario ที่เป็นไปไม่ได้
- ถ้าเขียนไป 200 บรรทัดแต่ทำ 50 บรรทัดได้ ให้เขียนใหม่

**3. แก้แบบผ่าตัด (Surgical) + รักษา Data-driven Architecture**
- แตะเฉพาะจุดที่จำเป็น ไม่ "ปรับปรุง" โค้ด/comment/format ข้างเคียงที่ไม่เกี่ยว
- ไม่ refactor สิ่งที่ไม่ได้พัง เขียนตาม style เดิมของโปรเจคแม้ไม่ถูกใจ
- เจอ dead code เดิม ให้แจ้ง ไม่ลบเอง (ลบเฉพาะ import/variable ที่การแก้ของตัวเองทำให้ไม่ถูกใช้)
- อย่าสร้างระบบซ้ำกับของเดิมโดยไม่ตรวจก่อนว่ามีอยู่แล้วหรือไม่
- `.tres` = balance, `.gd` = behavior, `.tscn` = composition — ห้าม hard-code base stat ใน entity ถ้ามี Data Resource รองรับอยู่แล้ว
- ห้ามเปลี่ยนชื่อ asset โดยพลการ
- ห้ามลบไฟล์เพื่อแก้ error โดยไม่ตรวจ dependency ก่อน
- ทุกบรรทัดที่แก้ต้องโยงกลับไปที่คำขอของ user ได้

**4. ทำงานแบบมีเป้าหมายตรวจสอบได้**
- แปลงงานเป็นเกณฑ์ verify ได้ก่อนเริ่ม เช่น "แก้บั๊ก" → "หา repro case ก่อน แล้วค่อยแก้ให้ผ่าน"
- งานหลายขั้นตอน ให้บอกแผนสั้นๆ พร้อมจุด verify แต่ละขั้น
- หลังแก้โค้ดทุกครั้ง ต้องรัน Parser + Runtime check แบบ headless อย่างน้อย ก่อนถือว่าเสร็จ

รายละเอียดเชิงลึกที่ไม่จำเป็นต่องานปัจจุบันไม่ควรถูกเติมเอง — ให้ตรวจจาก Project หรือถามผู้พัฒนาเมื่อจำเป็น
